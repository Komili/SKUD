# Система Контроля и Управления Доступом (СКУД) «FAVZ-GROUP»

<p align="center">
  <strong>Полноценное веб-приложение для учета рабочего времени и посещаемости сотрудников.</strong>
  <br />
  Интеграция с терминалами Hikvision, real-time уведомления в Telegram и удобные отчеты.
</p>

---

## 🚀 Ключевые возможности

- **📊 Панель управления (Dashboard):** Визуализация ключевых показателей и статистики посещаемости.
- **👥 Управление сотрудниками (CRUD):** Полный цикл управления данными сотрудников, включая добавление, редактирование, удаление и загрузку фотографий.
- **📄 Детальные отчеты:** Генерация и экспорт отчетов о посещаемости в форматах **PDF** и стилизованном **XLSX** с профессиональным форматированием.
- **🔍 Фильтрация и поиск:** Мгновенный поиск по сотрудникам и гибкая фильтрация по статусу (активен, в отпуске, уволен).
- **⚙️ Интеграция с Hikvision:** Автоматическая фиксация событий входа/выхода с терминалов доступа.
- **🔔 Telegram-уведомления:** Мгновенные оповещения о действиях сотрудников в корпоративный чат с детализацией офиса, двери и статуса (вход/выход/неизвестный сотрудник).
- **🔐 Аутентификация:** Безопасный вход в систему для администраторов с использованием хеширования паролей.

---

## 🛠️ Стек технологий

<p align="center">
  <img src="https://img.shields.io/badge/React-61DAFB?style=for-the-badge&logo=react&logoColor=black" alt="React"/>
  <img src="https://img.shields.io/badge/Node.js-339933?style=for-the-badge&logo=node.js&logoColor=white" alt="Node.js"/>
  <img src="https://img.shields.io/badge/Express.js-000000?style=for-the-badge&logo=express&logoColor=white" alt="Express.js"/>
  <img src="https://img.shields.io/badge/MySQL-4479A1?style=for-the-badge&logo=mysql&logoColor=white" alt="MySQL"/>
  <img src="https://img.shields.io/badge/Bootstrap-563D7C?style=for-the-badge&logo=bootstrap&logoColor=white" alt="Bootstrap"/>
  <img src="https://img.shields.io/badge/JavaScript-F7DF1E?style=for-the-badge&logo=javascript&logoColor=black" alt="JavaScript"/>
</p>

| Категория      | Технология                                                                                             |
| -------------- | ------------------------------------------------------------------------------------------------------ |
| **Бэкенд**     | `Node.js`, `Express.js`, `MySQL2`, `Multer` (загрузка файлов), `Bcrypt` (хеширование), `node-telegram-bot-api` |
| **Фронтенд**   | `React`, `React Bootstrap`, `Axios`, `React Router`, `React Datepicker`                                  |
| **Отчеты**     | `ExcelJS`, `FileSaver`, `jsPDF`, `jspdf-autotable`                                                      |
| **База данных**| `MySQL`                                                                                                |

---

## ⚙️ Установка и запуск

Выполните эти шаги для развертывания проекта на локальной машине.

### 1. Предварительные требования

- **Node.js** (v14+): [скачать](https://nodejs.org/)
- **MySQL Server** (v8+): [скачать](https://dev.mysql.com/downloads/mysql/)

### 2. Настройка базы данных

1.  Подключитесь к вашему MySQL серверу.
2.  Создайте базу данных `skud` с правильной кодировкой.
    ```sql
    CREATE DATABASE skud CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
    ```
3.  Переключитесь на созданную базу данных.
    ```sql
    USE skud;
    ```
4.  Выполните SQL-скрипт ниже для создания всех необходимых таблиц:
    ```sql
    -- Таблица компаний
    CREATE TABLE companies (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL UNIQUE
    );

    -- Таблица отделов
    CREATE TABLE departments (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL UNIQUE
    );

    -- Таблица пользователей (для входа в систему)
    CREATE TABLE users (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        username VARCHAR(255) NOT NULL UNIQUE,
        password VARCHAR(255) NOT NULL
    );

    -- Основная таблица сотрудников
    CREATE TABLE employees (
        id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        fullName VARCHAR(255) NOT NULL,
        position VARCHAR(255),
        phoneNumber VARCHAR(50) UNIQUE,
        email VARCHAR(255) UNIQUE,
        photoUrl VARCHAR(255),
        status VARCHAR(50) DEFAULT 'Активен',
        dateOfBirth DATE,
        hireDate DATE,
        companyId INT,
        departmentId INT,
        FOREIGN KEY (companyId) REFERENCES companies(id) ON DELETE SET NULL,
        FOREIGN KEY (departmentId) REFERENCES departments(id) ON DELETE SET NULL
    );

    -- Таблица для логов посещаемости
    CREATE TABLE attendance_logs (
        id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        employeeId BIGINT NOT NULL,
        checkin DATETIME,
        checkout DATETIME,
        FOREIGN KEY (employeeId) REFERENCES employees(id) ON DELETE CASCADE
    );
    ```

### 3. Настройка сервера (Бэкенд)

1.  Перейдите в директорию `server` и установите зависимости:
    ```bash
    cd server
    npm install
    ```
2.  Откройте файл `server/server.js` и **обязательно** укажите ваши данные:
    > **Важно:** Не храните реальные учетные данные в коде, если проект будет публичным. Используйте переменные окружения.

    ```javascript
    // Настройки MySQL
    const mysqlConfig = {
        host: '127.0.0.1',
        port: 3306,
        user: 'skud',      // <-- Ваш пользователь MySQL
        password: 'skud',  // <-- Ваш пароль
        database: 'skud',
        charset: 'utf8mb4' // <-- Важно для поддержки кириллицы
    };

    // Настройки Telegram
    const TELEGRAM_TOKEN = 'ВАШ_ТЕЛЕГРАМ_ТОКЕН';
    const TELEGRAM_CHAT_ID = 'ВАШ_ID_ЧАТА';
    ```

### 4. Настройка клиента (Фронтенд)

1.  В **новом окне терминала** перейдите в директорию `client` и установите зависимости:
    ```bash
    cd client
    npm install
    ```

### 5. Запуск приложения

1.  **Запустите сервер:**
    ```bash
    # В терминале с директорией /server
    node server.js
    ```
    > Сервер будет запущен на `http://localhost:3001`.

2.  **Запустите клиент:**
    ```bash
    # В терминале с директорией /client
    npm start
    ```
    > Приложение откроется в браузере по адресу `http://localhost:3000`.

---

## 🚀 Клонирование и сборка из GitHub

Если вы хотите развернуть копию этого проекта из репозитория GitHub, следуйте этим шагам.

### 1. Клонирование репозитория

Откройте терминал и выполните команду:
```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
cd YOUR_REPOSITORY
```
> Замените `YOUR_USERNAME` и `YOUR_REPOSITORY` на актуальные данные.

### 2. Установка зависимостей

В проекте две основные директории (`client` и `server`), и для каждой нужно установить свои зависимости.

1.  **Установка зависимостей сервера:**
    ```bash
    cd server
    npm install
    ```

2.  **Установка зависимостей клиента:**
    ```bash
    cd ../client 
    npm install
    ```

### 3. Настройка и запуск

После установки всех пакетов, вернитесь к основной инструкции в разделе **[⚙️ Установка и запуск](#️-установка-и-запуск)**, чтобы настроить базу данных, указать данные в `server/server.js` и запустить проект.

---

## 🚀 Подготовка к загрузке на GitHub

Чтобы загрузить этот проект в ваш собственный репозиторий GitHub, выполните следующие шаги.

### 1. Создайте новый репозиторий на GitHub

- Перейдите на [GitHub](https://github.com) и создайте новый пустой репозиторий.
- **Не инициализируйте** его с `README`, `.gitignore` или лицензией, так как эти файлы уже есть в проекте.

### 2. Свяжите локальный проект с удаленным репозиторием

Выполните в корневой директории проекта:
```bash
# Инициализация Git, если еще не сделано
git init

# Добавление всех файлов в отслеживание
git add .

# Создание первого коммита
git commit -m "Initial commit: project setup"

# Связывание с вашим репозиторием
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
```
> Замените `YOUR_USERNAME` и `YOUR_REPOSITORY` на ваши данные.

### 3. Загрузите файлы

Выполните команду, чтобы отправить все файлы в ваш репозиторий на GitHub:
```bash
git push -u origin master
```
> В зависимости от настроек GitHub, основная ветка может называться `main` вместо `master`.

---

## 📁 Структура проекта

```
.
├── client/         # Фронтенд (React)
│   ├── public/
│   └── src/
│       ├── components/ # Переиспользуемые компоненты
│       └── pages/      # Основные страницы
├── server/         # Бэкенд (Node.js)
│   ├── uploads/      # Папка для фото сотрудников
│   ├── server.js     # Основной файл API
│   └── isup-listener.js # Скрипт для Hikvision
└── README.md       # Этот файл
```

---

## 🌐 Сетевая архитектура для двух офисов

Эта система настроена для централизованного сбора данных с терминалов FaceID, расположенных в двух географически разделенных офисах. Сервер находится в Офисе 1.

### Офис 1 (Основной, где расположен сервер)

*   **Внешний IP-адрес:** `185.177.0.140`
*   **Локальный IP-адрес сервера:** `192.168.1.108` (на этой машине запущен Node.js сервер)
*   **Настройка роутера (Port Forwarding):** Внешний порт `7660` перенаправляется на внутренний IP-адрес сервера `192.168.1.108` (предположительно, на порт `3001`, на котором работает бэкенд).
*   **IP-адреса терминалов FaceID:**
    *   `192.168.1.190` (Вход, "outside")
    *   `192.168.1.191` (Выход, "inside")

### Офис 2 (Удаленный)

*   **Внешний IP-адрес:** `91.218.161.76`
*   **IP-адреса терминалов FaceID:**
    *   `192.168.0.161` (Вход, "outside")
    *   `192.168.0.160` (Выход, "inside")

### Принцип работы и настройка терминалов

1.  **Терминалы в Офисе 1** (в той же локальной сети, что и сервер) должны быть настроены на отправку событий (callback) на локальный IP-адрес сервера:
    *   **Адрес для колбека:** `http://192.168.1.108:3001/api/hikvision/event`

2.  **Терминалы в Офисе 2** (удаленный офис) должны быть настроены на отправку событий на **внешний** IP-адрес Офиса 1, используя настроенный порт `7660`:
    *   **Адрес для колбека:** `http://185.177.0.140:7660/api/hikvision/event`

Роутер в Офисе 1 примет запрос на порт `7660` и перенаправит его на локальный сервер `192.168.1.108:3001`, который обработает событие.

---

---

## 🔐 Настройка администратора

Для создания или обновления пользователя-администратора в системе используйте специальный скрипт:

1.  Перейдите в директорию `server`:
    ```bash
    cd server
    ```
2.  Запустите скрипт `add-admin.js`:
    ```bash
    node add-admin.js
    ```
    Этот скрипт создаст пользователя `admin` с паролем `admin` (или обновит пароль, если пользователь уже существует), безопасно хешируя его.

### Ручное добавление пользователя (для продвинутых пользователей)

Если вам нужно добавить пользователя вручную через базу данных, следуйте этим шагам:

1.  **Сгенерируйте хеш пароля:**
    *   Создайте временный файл (например, `hash-password.js`) в директории `server`.
    *   Вставьте в него следующий код, заменив `'ваш_пароль'` на нужный:
        ```javascript
        const bcrypt = require('bcrypt');
        const saltRounds = 10;
        const password = 'ваш_пароль'; // <-- ВАШ ПАРОЛЬ ЗДЕСЬ

        bcrypt.hash(password, saltRounds).then(hash => {
            console.log('Ваш хеш пароля:');
            console.log(hash);
        });
        ```
    *   Запустите скрипт: `node hash-password.js`.
    *   Скопируйте полученный хеш.
2.  **Вставьте пользователя в базу данных:**
    *   Подключитесь к вашей базе данных MySQL.
    *   Выполните SQL-запрос, заменив `'новое_имя'` и `'вставьте_хеш_сюда'`:
        ```sql
        INSERT INTO users (username, password) VALUES ('новое_имя', 'вставьте_хеш_сюда');
        ```

---

## ✨ Улучшения и исправления

В ходе разработки были внесены следующие важные изменения и улучшения:

*   **Исправлено отображение фотографий сотрудников:** Устранена проблема, из-за которой фотографии сотрудников не отображались корректно на страницах списка и деталей. Теперь URL-адреса изображений формируются правильно, используя прокси клиентского приложения.
*   **Улучшенные Telegram-уведомления:**
    *   Уведомления о входе и выходе теперь используют соответствующие эмодзи (🟢 для входа, 🔴 для выхода) и более информативный текст.
    *   Уведомления о удаленной разблокировке двери также стали более детализированными.
    *   В сообщениях теперь четко указывается **Офис** и **Дверь**, с которой произошло событие.
*   **Обработка неизвестных сотрудников:** Добавлена логика, которая предотвращает падение сервера при получении событий от терминалов для сотрудников, отсутствующих в базе данных. Вместо этого отправляется специальное уведомление в Telegram (`⚠️ *Обнаружен неизвестный сотрудник*`), информирующее о такой ситуации.
*   **Исправлена отправка уведомлений о входе/выходе:** Устранена ошибка, из-за которой уведомления о входе и выходе не отправлялись в Telegram из-за неопределенных переменных в обработчике событий.

---

## 🚑 Решение проблем (FAQ)

### 1. Ошибка: `Field 'id' doesn't have a default value`

- **Причина:** У колонки `id` в таблице `employees` отсутствует свойство `AUTO_INCREMENT`.
- **Решение:** Выполните SQL-запрос:
  ```sql
  ALTER TABLE employees MODIFY COLUMN id BIGINT NOT NULL AUTO_INCREMENT;
  ```

### 2. Ошибка: `Error Code: 1833` при изменении `employees.id`

- **Причина:** База данных блокирует изменение колонки `employees.id`, так как на нее ссылается таблица `attendance_logs` (foreign key).
- **Решение:** Необходимо временно отключить, а затем восстановить связь. Выполните 3 команды по порядку:

  1.  **Удаляем связь:**
      ```sql
      ALTER TABLE attendance_logs DROP FOREIGN KEY attendance_logs_ibfk_1;
      ```
  2.  **Добавляем `AUTO_INCREMENT`:**
      ```sql
      ALTER TABLE employees MODIFY COLUMN id BIGINT NOT NULL AUTO_INCREMENT;
      ```
  3.  **Возвращаем связь:**
      ```sql
      ALTER TABLE attendance_logs ADD CONSTRAINT attendance_logs_ibfk_1 FOREIGN KEY (employeeId) REFERENCES employees(id) ON DELETE CASCADE;
      ```

### 3. Проблема: Кириллица отображается как `????`

- **Причина:** Неправильная кодировка при подключении к БД.
- **Решение:** Убедитесь, что в `server/server.js` в `mysqlConfig` указан `charset: 'utf8mb4'`, как показано в инструкции по настройке.

### 4. Как полностью очистить данные сотрудников и посещаемости?

- **Причина:** Необходимо сбросить тестовые данные для чистого запуска.
- **Решение:** Выполните SQL-запросы для очистки таблиц, временно отключив проверку внешних ключей.
  ```sql
  SET FOREIGN_KEY_CHECKS = 0;
  TRUNCATE TABLE skud.attendance_logs;
  TRUNCATE TABLE skud.employees;
  SET FOREIGN_KEY_CHECKS = 1;
  ```
