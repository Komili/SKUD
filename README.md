# Система Контроля и Управления Доступом (СКУД) «FAVZ-GROUP»

## 🚀 Быстрый старт

1.  **Настройте бэкенд:**
    ```bash
    cd server
    npm install
    # Отредактируйте server.js, указав данные для подключения к БД
    node server.js
    ```

2.  **Настройте фронтенд (в новом окне терминала):**
    ```bash
    cd client
    npm install
    npm start
    ```
> Подробные инструкции по настройке базы данных и решению проблем находятся ниже.

---

Это полноценное веб-приложение, разработанное для учета сотрудников и мониторинга их посещаемости. Система состоит из клиентской части (React) и серверной части (Node.js), интегрируется с оборудованием Hikvision для автоматического отслеживания событий входа/выхода и отправляет уведомления в Telegram.

## 🚀 Основные возможности

- **Панель управления (Dashboard):** Обзор ключевых показателей и статистики.
- **Управление сотрудниками (CRUD):**
  - Добавление, редактирование и удаление сотрудников.
  - Загрузка и отображение фотографий.
  - Просмотр детальной информации по каждому сотруднику.
- **Отчеты о посещаемости:**
  - Генерация отчетов по сотрудникам за выбранный период.
  - Фильтрация по компании и поиск по имени.
- **Экспорт отчетов:** Выгрузка отчетов о посещаемости в форматах PDF и стилизованном XLSX с профессиональным форматированием.
- **Фильтрация и поиск:** Удобный поиск по сотрудникам и фильтрация по их статусу (активен, в отпуске, уволен).
- **Интеграция с Hikvision:**
  - Прослушивание событий с терминалов доступа (вход/выход).
  - Автоматическая запись событий в базу данных.
- **Telegram-уведомления:** Мгновенные оповещения о событиях входа/выхода в указанный чат.
- **Аутентификация:** Защищенный вход в систему для администраторов.

---

## 🛠️ Стек технологий

### Бэкенд
- **Node.js:** Среда выполнения JavaScript.
- **Express.js:** Веб-фреймворк для создания API.
- **MySQL2:** Драйвер для работы с базой данных MySQL.
- **Multer:** Middleware для обработки загрузки файлов (фотографий).
- **Bcrypt:** Хеширование паролей для безопасного хранения.
- **node-telegram-bot-api:** Интеграция с Telegram Bot API.

### Фронтенд
- **React:** Библиотека для создания пользовательских интерфейсов.
- **React Bootstrap:** Адаптивные UI-компоненты.
- **Axios:** Клиент для выполнения HTTP-запросов к серверу.
- **React Router:** Навигация и маршрутизация в приложении.
- **React Datepicker:** Компонент для выбора дат.
- **ExcelJS & FileSaver:** Создание и скачивание стилизованных `.xlsx` отчетов.
- **jsPDF & jspdf-autotable:** Создание `.pdf` отчетов.

### База данных
- **MySQL:** Реляционная система управления базами данных.

---

## ⚙️ Установка и запуск

Для развертывания проекта на локальной машине выполните следующие шаги.

### 1. Предварительные требования

Убедитесь, что у вас установлены:
- **Node.js** (версия 14 или выше): [скачать](https://nodejs.org/)
- **MySQL Server** (версия 8 или выше): [скачать](https://dev.mysql.com/downloads/mysql/)

### 2. Настройка базы данных

1.  Подключитесь к вашему MySQL серверу.
2.  Создайте новую базу данных с именем `skud`.

    ```sql
    CREATE DATABASE skud CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
    ```
3.  Переключитесь на созданную базу данных.
    ```sql
    USE skud;
    ```
4.  Выполните следующий SQL-скрипт для создания всех необходимых таблиц:

    ```sql
    -- Таблица компаний
    CREATE TABLE companies (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL UNIQUE
    );

    -- Таблица отделов
    CREATE TABLE departments (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL UNIQUE
    );

    -- Таблица пользователей ( для входа в систему )
    CREATE TABLE users (
        id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        username VARCHAR(255) NOT NULL UNIQUE,
        password VARCHAR(255) NOT NULL
    );

    -- Основная таблица сотрудников
    CREATE TABLE employees (
        id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        fullName VARCHAR(255) NOT NULL,
        position VARCHAR(255),
        phoneNumber VARCHAR(50) UNIQUE,
        email VARCHAR(255) UNIQUE,
        photoUrl VARCHAR(255),
        status VARCHAR(50) DEFAULT 'Активен',
        dateOfBirth DATE,
        hireDate DATE,
        companyId INT,
        departmentId INT,
        FOREIGN KEY (companyId) REFERENCES companies(id) ON DELETE SET NULL,
        FOREIGN KEY (departmentId) REFERENCES departments(id) ON DELETE SET NULL
    );

    -- Таблица для логов посещаемости
    CREATE TABLE attendance_logs (
        id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
        employeeId BIGINT NOT NULL,
        checkin DATETIME,
        checkout DATETIME,
        FOREIGN KEY (employeeId) REFERENCES employees(id) ON DELETE CASCADE
    );
    ```

### 3. Настройка сервера (Бэкенд)

1.  Перейдите в директорию сервера:
    ```bash
    cd server
    ```
2.  Откройте файл `server.js` и **обязательно** измените следующие параметры на ваши:

    - **Настройки MySQL** (данные для подключения к вашей базе данных):
      ```javascript
      const mysqlConfig = {
          host: '127.0.0.1',
          port: 3306,
          user: 'skud',      // <-- Ваш пользователь MySQL
          password: 'skud',  // <-- Ваш пароль
          database: 'skud'
      };
      ```
    - **Настройки Telegram** (если вы хотите использовать уведомления):
      ```javascript
      const TELEGRAM_TOKEN = 'ВАШ_ТЕЛЕГРАМ_ТОКЕН';
      const TELEGRAM_CHAT_ID = 'ВАШ_ID_ЧАТА';
      ```

3.  Установите все необходимые зависимости:
    ```bash
    npm install
    ```

### 4. Настройка клиента (Фронтенд)

1.  В отдельном терминале перейдите в директорию клиента:
    ```bash
    cd client
    ```
2.  Установите все зависимости:
    ```bash
    npm install
    ```

### 5. Запуск приложения

1.  **Запустите сервер:**
    В терминале, где открыта папка `server`, выполните команду:
    ```bash
    node server.js
    ```
    Вы должны увидеть сообщение об успешном подключении к БД и запуске сервера на порту 3001.

2.  **Запустите клиент:**
    В терминале, где открыта папка `client`, выполните команду:
    ```bash
    npm start
    ```
    После этого в вашем браузере автоматически откроется страница приложения по адресу [http://localhost:3000](http://localhost:3000).

---

## 📁 Структура проекта

- **`/client`**: Исходный код фронтенд-приложения на React.
  - **`/src/components`**: Переиспользуемые компоненты (модальные окна, навигация).
  - **`/src/pages`**: Основные страницы приложения (логин, список сотрудников, отчеты).
- **`/server`**: Исходный код бэкенд-приложения на Node.js.
  - **`/uploads`**: Папка, куда сохраняются фотографии сотрудников.
  - `server.js`: Основной файл сервера, описывающий всю логику API.
  - `isup-listener.js`: Скрипт для прослушивания событий от терминалов Hikvision.
- **`README.md`**: Этот файл.

---

## 🚑 Возможные проблемы и их решения

Если вы столкнулись с ошибками при установке или работе с приложением, проверьте этот раздел.

### 1. Ошибка: `Field 'id' doesn't have a default value`
- **Причина:** Эта ошибка возникает при попытке добавить нового сотрудника, если у колонки `id` в таблице `employees` не установлено свойство `AUTO_INCREMENT`.
- **Решение:** Выполните следующий SQL-запрос в вашей базе данных `skud`, чтобы исправить структуру таблицы:
  ```sql
  ALTER TABLE employees MODIFY COLUMN id INT NOT NULL AUTO_INCREMENT;
  ```

### 2. Ошибка: `Multiple primary key defined`
- **Причина:** Вы пытаетесь установить первичный ключ (`PRIMARY KEY`) для колонки, у которой он уже есть.
- **Решение:** Используйте более простую команду из пункта 1, которая только добавляет `AUTO_INCREMENT`, не затрагивая ключ.

### 3. Проблемы с кириллицей (отображается как `????`)
- **Причина:** Неправильная кодировка при подключении к базе данных.
- **Решение:** Убедитесь, что сама база данных создана с кодировкой `utf8mb4` (как указано в инструкции). Также в файле `server/server.js` в объекте `mysqlConfig` должен быть указан параметр `charset`:
  ```javascript
  const mysqlConfig = {
      // ...
      charset: 'utf8mb4'
  };
  ```

### 4. Ошибка: `Incorrect date value`
- **Причина:** Сервер пытается записать в базу данных дату в формате, отличном от `YYYY-MM-DD`.
- **Решение:** Эта проблема уже исправлена в коде сервера. Если вы столкнулись с ней, убедитесь, что используете последнюю версию кода, где даты перед сохранением форматируются с помощью `.toISOString().split('T')[0]`.




Ошибка Error Code: 1833 возникает именно     
  потому, что колонка employees.id используется как "связь" (foreign key) в другой таблице —
  attendance_logs. База данных не дает вам изменить колонку, на которую ссылаются другие    
  таблицы, чтобы не нарушить целостность данных.

  Это стандартная мера безопасности. Чтобы ее обойти, нам нужно временно убрать эту связь, 
  внести изменения и затем вернуть связь обратно.

  Вот 3 команды, которые нужно выполнить в вашей базе данных `skud` по порядку:

  Шаг 1: Временно удаляем связь (foreign key)
  Эта команда уберет ограничение, которое мешает нам.

   1 ALTER TABLE attendance_logs DROP FOREIGN KEY attendance_logs_ibfk_1;

  Шаг 2: Добавляем `AUTO_INCREMENT` к таблице сотрудников
  Теперь, когда таблица не "заблокирована", эта команда должна выполниться без ошибок. Вы  
  правильно заметили, что тип данных BIGINT.

   1 ALTER TABLE employees MODIFY COLUMN id BIGINT NOT NULL AUTO_INCREMENT;

  Шаг 3: Возвращаем связь на место
  Эта команда восстановит связь между таблицами, чтобы ваша база данных оставалась
  целостной.

   1 ALTER TABLE attendance_logs ADD CONSTRAINT attendance_logs_ibfk_1 FOREIGN KEY
     (employeeId) REFERENCES employees(id) ON DELETE CASCADE;
  Последовательное выполнение этих трех команд решит проблему. После этого вы сможете
  запустить скрипт node seed.js без ошибок.